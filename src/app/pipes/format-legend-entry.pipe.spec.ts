import { FormatLegendEntryPipe } from './format-legend-entry.pipe';

describe('FormatLegendEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLegendEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
